server {
  listen       8080;
  listen       [::]:8080;
  server_name  _;

  root   /usr/share/nginx/html;
  index  index.html;

  # Serve all files as static assets
  location / {
    try_files $uri $uri/ =404;
  }
}

